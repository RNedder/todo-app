const Header = () => {
    return (
        <h1>
            <span className='badge bg-dark'>to do</span>
            <small className='text-muted'>app</small>
        </h1>
    );
};

export default Header;